{
  "name": "revisego",
  "version": "1.0.0",
  "description": "Revision, but actually fun — an arcade for GCSE revision.",
  "private": true,
  "scripts": {
    "test": "node tests/smoke.js"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
